## Accessibility

When `search` or `clear` is enabled, `w-affix` renders a button. Always provide an `aria-label` describing the action.

```html
<w-affix search aria-label="Search"></w-affix>
<w-affix clear aria-label="Clear input"></w-affix>
```
